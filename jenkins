pipeline {

    agent any 
       def name = "${env.BRANCH_NAME}"

    stages {

        stage ('build'){
            when {
                branch 'dev'
            }
            steps {
                echo 'Working on dev branch   --- $name'
            }
        }
    }
}
